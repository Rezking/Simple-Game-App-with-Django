{
    "builds": [{
        "src": "django_project/wsgi.py",
        "config": { "runtime": "python3.8" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "django_project/wsgi.py"
        }
    ]
}
